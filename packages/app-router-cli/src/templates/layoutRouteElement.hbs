<LayoutRouteElement
  {{~#unless (isNil props.loading)}} loading={ {{~escape props.loading~}} }{{/unless}}
  {{~#unless (isNil props.error)}} error={ {{~escape props.error~}} }{{/unless}}
  {{~#unless (isNil props.notFound)}} notFound={ {{~escape props.notFound~}} }{{/unless}}
  {{~#unless (isNil props.template)}} template={ {{~lazyImport props.template~}} }{{/unless~}}
  {{~#unless (isNil props.parallelRoutes)}} parallelRoutes={ {{~parallelRoutesProps props.parallelRoutes~}} }{{/unless~}}
  {{~#unless (isNilorPreset props.layout)}}>{ {{~lazyImport props.layout~}} }</LayoutRouteElement>{{else}}/>{{/unless~}}

