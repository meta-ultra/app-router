import { type Metadata, type GenerateMetadata } from "../../../../../../src";

export const metadata: Metadata = {
  title: "Profile",
  applicationName: "auth example",
};

const Profile = () => {
  return <div>Profile</div>;
};

export default Profile;
